-r requirements.txt

# --- Testing ---
pytest==9.1.1
pytest-cov==7.1.0
# Starlette's TestClient prefers httpx2; httpx is kept because the installed
# FastAPI still imports it on some paths.
httpx==0.28.1
httpx2==2.13.0

# --- Lint / security scanning ---
ruff==0.16.7
bandit==1.9.4
pip-audit==2.10.1

# --- ML test doubles ---
# The embedding-based modules (embedding_segmenter.py, transformer_extractor.py)
# operate on numpy arrays returned by sentence-transformers. Tests mock the
# encode call itself (never a real model download -- see requirements-ml.txt
# for that), but still need real array semantics (@, .mean(), .argmax()) to
# exercise the boundary-detection and classification math meaningfully. numpy
# alone is a small, fast install with no model weights, unlike the packages in
# requirements-ml.txt.
numpy==2.3.4
